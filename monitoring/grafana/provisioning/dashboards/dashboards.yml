apiVersion: 1

providers:
  - name: RunApp
    orgId: 1
    folder: RunApp
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
